import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 20,
    backgroundColor: "white",
    borderRadius: 10,
    marginTop: 10,
  },

  button: {
    backgroundColor: "#1562c6",
    borderRadius: 20,
    height: 30,
    width: 100,
    alignItems: "center",
    justifyContent: "center",
    alignSelf: "center",
  },
  title: {
    fontSize: 16,
    fontWeight: "bold",
  },
});
