import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    gap: 30,
    backgroundColor: "transparent",
    paddingBottom: 10,
  },

  empty: {
    alignSelf: "center",
    fontSize: 20,
    fontWeight: "bold",
  },
});
